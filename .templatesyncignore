# .templatesyncignore
#
# Files that must NOT be overwritten when syncing from the upstream
# ludeeus/integration_blueprint template. Uses .gitignore-style glob patterns.
#
# The upstream sync pulls with "-X theirs" (upstream wins on conflict), so this
# ignore list is the ONLY thing protecting your own code and identity. Keep it
# accurate. Full docs: .github/template-sync.md

# --- Your integration code (never sync: it diverges the moment you build it) ---
custom_components/**

# --- Per-repo identity (yours, not the template's) ---
manifest.json
hacs.json
README.md
CONTRIBUTING.md
.devcontainer.json
config/configuration.yaml
.github/ISSUE_TEMPLATE/**

# Note on LICENSE: it is intentionally NOT ignored. Upstream stores it (and a few
# other files) with CRLF while .gitattributes normalizes to LF, which used to
# produce whole-file line-ending noise. That is now handled generally by the
# `git add --renormalize .` precommit step in the workflow, so a genuine upstream
# LICENSE change still syncs cleanly. (If you customize your LICENSE, add it here.)

# Note: .templatesyncignore itself is never synced (the action restores it).
